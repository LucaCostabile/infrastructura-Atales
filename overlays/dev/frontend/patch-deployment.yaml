apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  template:
    spec:
      containers:
        - name: frontend
          image: luca011/frontend-atales:dev-v2-20250805193913
          env:
            - name: NODE_ENV
              value: "development"
            - name: DEBUG
              value: "true"

