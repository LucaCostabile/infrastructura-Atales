apiVersion: apps/v1
kind: Deployment
metadata:
  name: business-service
spec:
  template:
    spec:
      containers:
        - name: business-service
          image: luca011/business-service-atales:dev-latest  # CI/CD reemplaza esto
          env:
            - name: NODE_ENV
              value: "development"
            - name: DEBUG
              value: "true"

